#include <stdio.h>
/**
 * main - C program prints a line using the function puts
 * Return: 0 The program should end with 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
